//========== Landscape or Portrait ===========
//There's no need to write this part (? true : false).

function isLandscape(width, height) {
  return (width > height);
}
let result = isLandscape(30, 40);
console.log(result);
